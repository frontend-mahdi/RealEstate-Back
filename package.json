{
  "name": "RealEstate",
  "version": "1.0.0",
  "description": "",
  "main": "source/server.ts",
  "scripts": {
    "build": "rm -rf build && npx prettier --write source/ && npx tsc",
    "bStart": "rm -rf build && npx prettier --write source/ && npx tsc && node ./build/server.js",
    "dev": "node ./build/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.15"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.8",
    "@types/mongoose": "^5.10.1",
    "typescript": "^5.2.2"
  }
}
